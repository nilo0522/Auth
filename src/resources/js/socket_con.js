import React from "react";


const Socket_Conn = (data) => {
    let ip_address = '127.0.0.1';
    let socket_port = '3000';
    let socket = io(ip_address + ':' + socket_port);
    
socket.emit()
alert('dasd')
}

export default Socket_Conn